# Project goal

<!--
This is the file you give to `bun run bin/autopilot --goal goal.md`.
Write what you want built; the autopilot's bootstrap council will
decompose it into leaf goals + frozen test specs.

Tips:
  - Be specific about user-visible behavior, vague about implementation
  - Mention any non-negotiable constraints (frameworks, no new deps, etc.)
  - Mention what "done" looks like from a user perspective
  - Don't list sub-goals yourself; the council does that better than you do
-->

## What I want built

<!-- One paragraph describing the feature or system in user-visible terms. -->

## Constraints

<!-- Hard requirements the implementation must satisfy.
     Examples: "must run on Bun", "no new npm dependencies",
               "must not modify <file>", "must keep API backward-compatible". -->

## What "done" looks like

<!-- A concrete user-visible outcome.
     Examples: "I can run `tool foo bar` and see X",
               "Visiting /api/things returns Y on a POST". -->

## Out of scope

<!-- Optional: things you explicitly DON'T want done in this run.
     Helps the council avoid scope creep during decomposition. -->
